// Create app demo Shortcuts class

import 'package:flutter/material.dart';

class ShortcutsDemo extends StatelessWidget {
  const ShortcutsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Shortcuts'),
      ),
      body: const Center(
        child: Text('Shortcuts'),
      ),
    );
  }
}

// Path: lib/shortcut_paste.dart
// Create app demo Shortcuts class
