name: animations
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  device_preview: 1.2.0
  font_awesome_flutter: ^9.2.0 # Thay đổi phiên bản nếu cần
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  
 

flutter:
  uses-material-design: true
  assets:
    - images/flippers-alpha.png
    - images/
